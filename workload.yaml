endpoints:
  - name: backend-api
    port: 5000
    type: HTTP

dependencies:
  resources:
    - ref: order-db
      envBindings:
        DB_HOST: host
        DB_PORT: port
        DB_USER: username
        DB_PASSWORD: password
        DB_NAME: database